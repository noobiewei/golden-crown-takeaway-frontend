server {
    listen 80;
    server_name goldencrownwatford.co.uk www.goldencrownwatford.co.uk;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name goldencrownwatford.co.uk;

    ssl_certificate /etc/letsencrypt/live/goldencrownwatford.co.uk/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/goldencrownwatford.co.uk/privkey.pem;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
